from flab_bada.domain.lecture.lectures_repository import LectureRepository
from flab_bada.models.lectures import Lecture


def test_create_lecture():
    pass
    # name = "java"
    # doc = "기초 자바"
    # lr = LectureRepository()
    # ret_data = lr.create_lectures(Lecture(user_id=1, name=name, doc=doc))
